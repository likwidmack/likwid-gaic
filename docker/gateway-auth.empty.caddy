# No-op auth snippet for loopback. Replace with a hashed basic-auth block before
# leaving FORKEDAI_BIND_ADDRESS=127.0.0.1. See docs/network-security.md and
# docker/gateway-auth.basicauth.example.caddy.
(gateway_auth) {
}
