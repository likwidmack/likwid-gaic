# No-op auth snippet for loopback. Replace with a hashed password gate before
# leaving FORKEDAI_BIND_ADDRESS=127.0.0.1. See docs/network-security.md and the
# docker/gateway-auth example sibling file.
(gateway_auth) {
}
